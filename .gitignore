# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Local OpenCloudSkillHub working dirs (skill installs live in the user profile,
# but ignore any local stray ones too)
.opencloudskillhub/

# Feedback log (runtime-written; not source)
registry/feedback/*.jsonl

# Issued agent tokens (runtime-written secrets; never commit)
registry/tokens.json

# Deployment env file (may hold the admin token)
deploy/ocsh.env

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
